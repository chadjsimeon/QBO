kind = "web"
previewPath = "/"
title = "Ledgerly — Accounting"
version = "1.0.0"
id = "artifacts/ledgerly"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21074

[services.development]
run = "pnpm --filter @workspace/ledgerly run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ledgerly", "run", "build" ]
publicDir = "artifacts/ledgerly/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21074"
BASE_PATH = "/"
