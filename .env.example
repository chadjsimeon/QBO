# Postgres (matches docker-compose.yml)
DATABASE_URL="postgresql://qbo:qbo@localhost:5433/qbo?schema=public"

# Auth.js — generate with: npx auth secret   (or any random string)
AUTH_SECRET="dev-secret-change-me-in-production"
